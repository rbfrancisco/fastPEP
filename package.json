{
  "name": "fastpep",
  "version": "1.0.0",
  "description": "FastPEP - Prontuário Eletrônico Rápido para médicos de emergência",
  "main": "server/index.js",
  "scripts": {
    "server": "node server/index.js",
    "start": "node server/index.js",
    "split-data": "node scripts/split-data.js",
    "compile-data": "node scripts/compile-data.js",
    "validate-data": "node scripts/validate-data.js",
    "build": "npm run compile-data && npm run validate-data && node build.js",
    "build:full": "npm run compile-data && npm run validate-data && node build.js --include-admin"
  },
  "keywords": [
    "medical",
    "ehr",
    "emergency"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "pagecrypt": "^6.1.0"
  }
}
